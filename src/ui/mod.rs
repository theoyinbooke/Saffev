//! UI surface helpers for the CLI (the terminal counterpart to the Studio).
//!
//! Hosts the [`palette`] — the same tokens as `design/tokens.css`, so the CLI
//! and Studio never drift.

pub mod palette;
