# G4 round log — Observability surface

## Round 1 — 2026-07-28 — 34 attributes + the offline privacy report

**Builder artifact.** The per-request record widened 21 → **34 attributes**
in History detail (metadata-only, invariant intact): request shape (bytes,
User-Agent/Content-Type subset, sampled params temperature/top_p/max_tokens
with Ollama `options` nesting handled, message/system/tool counts), response
diagnostics (streamed bytes via a real tee counter — the capped buffer's
len() was wrong for big streams — total generation time, finish reason,
derived tokens/sec), engine version enriched on the detail path only, plus
the existing attribution/token/timing/PII/safety fields. Schema migration
v9; store round-trip + param-parser + DTO-derivation tests.

**`saffev report`** — a pure Markdown renderer (`src/report.rs`) + CLI: the
"What left this machine?" executive answer, traffic/apps/models, findings
by kind and by app, masking posture, exposure verdict, agent coverage,
archive integrity — with a stated **measurement boundary on every section**
and an explicit zero-network claim. Verified live against this machine's
store.

**Artifacts (CI-guarded, the G3 lesson):** `bench/observability-results.json`
(attribute table vs the Portkey 40+ bar) and `docs/examples/privacy-report.md`
(regenerated deterministically from fixed inputs) — `observability-bench.yml`
diffs both on every push.

**Critic verdict.** _pending — fresh-context critic below._

**Critic verdict (fresh-context, judged at 15f9f63).** **SOUND**, with a real
defect and honest deflation. Reproducibility PASS (artifacts byte-identical,
full suite green). Rubric (a): the critic traced all 34 names through all
four stages (proxy → schema → DTO → drawer) — **31 survive**: request_hash
was stored but never exposed/rendered (a dead name the bench falsely claimed
was rendered), and the two `_src` fields are provenance footnotes (the `~`
prefix), counted grudgingly. Bar met at 31 ≥ 30. Rubric (b): conditional
pass — the sample answers all five reviewer questions with boundaries, but
**the production exposure check probed the PROXY port while the verdict text
claims the ENGINE** (in Cooperative mode a world-bound engine would be
falsely reassured about); also: "went to a local engine" was a hardcoded
assertion wearing a measurement's clothes, the sample's generation date
(2025) contradicted its pricing date (2026), and agent/archive counts were
unscoped by period. Six probes all passed (codepoint-safe truncation,
migration nulls, division guards). Weakest section: **Network exposure**.

## Round 2 — 2026-07-28 — the critic's findings

Exposure now probes the mode-aware ENGINE port (shadow in Gateway, upstream
in Cooperative — mirroring `status`); the "local engine" line is restated as
the structural property it is; the sample's clock fixed (2026); the traffic
boundary states the 100k-row cap; agent/archive sections state their
all-time scope; request_hash is exposed and rendered in the drawer's
integrity row (making the bench's rendering claim TRUE rather than deleting
it).

**Closing critic verdict (fresh-context, judged at 59db049).** **PASS —
CONVERGED.** Both round-1 findings verified fixed end-to-end on a clean
clone: exposure now probes the mode-aware ENGINE port (Gateway→shadow,
Cooperative→upstream), and request_hash survives proxy→schema→DTO→render
(the drawer's integrity row). Independent count clears 30. Report: clock
internally consistent, a boundary on every section, offline verified (no
network primitives in the report path), all five reviewer questions
answerable. Four fresh probes clean (0-exchange renders sensibly,
Gateway uses shadow port, unwritable --out errors without panic). Weakest
section: Network exposure — and the committed sample carried a HAND-WRITTEN
exposure string the real code never emits.

**Round 3 (closing).** The sample report's exposure line now matches the
real `classify_binding` output verbatim ("Engine is bound to localhost
only, not reachable from the network."), so the committed doc is what the
tool actually prints — no fiction. G4 converged.

---

## G4 · CLOSED — 2026-07-28

Three rounds, two fresh-context critic passes. History detail carries **34
metadata attributes** (independently audited to clear the ≥30 bar; raw
content never stored unless payload storage is explicitly enabled), and
`saffev report` produces an offline Markdown privacy report answering "what
left this machine?" with a stated measurement boundary on every section.
Both critic findings (wrong-port exposure, unrendered request_hash) and the
closing cosmetic (sample/real string divergence) fixed. Artifacts
CI-guarded (`observability-bench.yml`): attribute table + a
deterministically-regenerated sample report.
