# G3 round log — Cost & usage analytics

## Round 1 — 2026-07-28 — the harness: ccusage parity measured, not asserted

**Builder artifact.** `tests/fixtures/costs/home/.claude` — a fixture home
(3 sessions, 6 assistant usage records across opus/sonnet/haiku, spanning
3 UTC days, with entry spacing that forces 5 usage blocks + 4 gap blocks).
`bench/ccusage-reference.json` — the committed output of the REAL ccusage
(v20.0.19, `--offline --timezone UTC`) run against that fixture, with the
isolation env documented inside it (ccusage merges every agent it detects;
CODEX_HOME/COPILOT_HOME/GEMINI_CLI_HOME/HOME must be redirected — its first
un-isolated run happily billed this machine's real codex history).
`src/agents/usage.rs` — the per-event engine: extraction from Claude Code
JSONL with ccusage's dedupe (message.id + requestId), UTC daily rows,
ccusage-compatible 5-hour billing blocks (start = entry ts floored to the
UTC hour; new block on window edge or ≥5h entry gap; synthetic gap blocks
from last-entry+5h to the next entry), live burn + projection on the active
block, and totals. Pricing v2: the table is now VERSIONED (`as_of`,
surfaced in artifacts) and prices cache READ and WRITE separately
(`lookup_split`; write = 1.25 × input Anthropic convention, derived when a
user table omits it) — pricing writes at the read rate understated the
fixture's opus day by ~26%. `tests/costs_bench.rs` compares Saffev to the
committed reference and emits `bench/costs-results.json`.

**Measured (this commit): max cost delta 0.0000% — full float-precision
match** on totals, every daily row, every per-model row, and all 9 blocks
(boundaries, gap-ness, entry counts EXACT). Token sums exact. The rubric
allows ±2%; the fixture needs none of it.

**Named gap.** Rubric (b) needs the blocks/burn surfaced in the Studio and
plan-limit progress; the fixture is small (6 events) and single-agent
(claude only — ccusage's own primary target, per the bar's wording).

**Critic verdict.** _pending — after round 2._

## Round 2 — 2026-07-28 — blocks, burn, and plan progress in the Studio

**Builder artifact.** `/api/agents/analytics` now carries the G3 usage
report: last 30 UTC daily rows, last 20 billing blocks (gaps included),
overall totals, and plan progress for the active block. Plan allowances are
honest by design: with no configuration the baseline is the user's highest
OBSERVED block (labeled "observed-max" — no invented dollar grants); setting
`pricing.plan` + `pricing.plan_block_allowance_usd` switches to the user's
own estimate, and the UI always says which one it is showing. The Usage tab
renders the active block's spend, live burn ($/h and tok/min), projection at
current pace, reset countdown, a plan progress bar, and the last 7 days
(cache reads and writes as separate columns), footnoted with the pricing
table's as-of date and the no-fetch invariant.

Mid-round interrupt, shipped as v0.7.1: the user reported the Linux Studio
update never applying — root-caused to `/proc/self/exe` reading
`"<path> (deleted)"` after the binary is replaced, making the restart
helper's script unrunnable, with its stderr silenced to /dev/null. Fixed
(`current_exe_on_disk`, helper output now logged), released.

**Critic verdict.** _pending — fresh-context critic below._

**Critic verdict (fresh-context, judged at 8e595f1).** **UNSOUND — one fatal,
self-inflicted.** The fixture home was never committed: `.gitignore`'s
`.claude/` rule (meant for the repo's own guidance dir) swallowed
`tests/fixtures/costs/home/.claude`, so the parity bench fails on every
clean clone — and no CI workflow existed to catch it. Everything else held:
the critic re-ran the REAL ccusage with the documented isolation env and got
an EXACT match against the committed reference on all four reports
("the numbers are real"); zero outbound calls verified. Nine probes found
three real divergences — records missing input_tokens (ccusage drops them,
saffev zero-defaulted), an entry exactly at block_start+5h (ccusage keeps it
in the block; saffev split), and stored `costUSD` records (ccusage's auto
mode trusts them; saffev recomputed — unbounded delta on old histories) —
plus two bar gaps: monthly missing entirely, per-project $/day missing, and
the session list still priced cache at the single read rate (two pricing
paths, different dollars).

## Round 3 — 2026-07-28 — the critic's findings, all closed

**Builder artifact.** The fatal: `.gitignore` rule root-anchored
(`/.claude/`), the fixture committed, and `costs-bench.yml` added to CI —
the exact guard whose absence let this ship. Semantics: records missing
input/output tokens are DROPPED (ccusage schema behavior), block boundaries
are strict-`>` (an entry at exactly start+5h stays in its block —
empirically verified), and stored `costUSD` is trusted (ccusage auto mode).
The fixture now CONTAINS all four adversarial shapes (missing-input record,
stored-cost record, boundary-exact entry, cross-file duplicate) with the
reference regenerated — ccusage's behavior on each matched the new
semantics exactly, and the bench pins each shape explicitly. Bar gaps:
`usage::monthly()` added, bench-compared against ccusage's monthly report,
exposed in the API; `DailyRow.by_project` carries $ per project per day.
Pricing paths unified: `AgentSession.cache_write_tokens` landed across all
12 adapters (7 formats split it: claude_code, opencode, copilot, cline,
goose, amp, roo), and every session-view cost site now prices reads and
writes separately via `cost_usd_split` — archived sessions (no split
stored) keep the read-rate estimate, documented in place.

**Measured (this commit): max cost delta 0.0000%** across totals, daily,
monthly, per-model, blocks, and sessions — with the adversarial shapes in
the fixture.

**Critic verdict.** _pending — closing critic below._

**Closing critic verdict (fresh-context, judged at 8fbfd86).** **PASS — G3
CONVERGED.** The round-1 fatal is demonstrably gone (4 fixture files
committed, bench passes on a clean clone, CI guard present); the reference
re-verified byte-equal against a live ccusage run; all three round-1
divergences confirmed fixed with independent probes; five fresh probes —
tokens-missing-with-costUSD, absurd stored costs, the gap condition at
exactly last+5h, year-boundary monthly, missing-cwd projects — ALL matched
live ccusage exactly. Rubric: (a) 0.0000% max delta, (b) blocks + live burn
present and active-only, (c) zero outbound verified. Corrections accepted:
the round-3 log said "cross-file duplicate" where the fixture's dup is
same-file (cross-file verified separately by the critic — both tools dedupe
globally); plan observed-max included the active block (bar pinned at 100%
whenever current was the record).

**Largest unexplained delta (named per the rubric).** The session-VIEW path
vs the usage path: session aggregates do not dedupe, do not drop
missing-token records, and ignore stored costUSD — on the adversarial
fixture session the views differ 6.11×. The rate formula and (post-close
fix) the price table are now shared; the remaining divergence is inherent
to session-level aggregation and sits OUTSIDE the ccusage-parity surface
the bar names. Logged as the follow-up, not a blocker.

## Round 4 — 2026-07-28 — closing fixes

Session-view cost sites use the LIVE configured price table
(`cost_usd_split_with`), removing the default-table shortcut; the plan
baseline is the max of COMPLETED blocks (with only one block in history,
100% is the honest answer). Documented boundaries: the usage engine reads
`~/.claude` only (the bar's literal wording; ccusage merges other agents),
parity is proven at UTC (ccusage's default is local time), and the burn
formula is Saffev's own (the reference pins only inactive blocks).

---

## G3 · CLOSED — 2026-07-28

Four rounds, two fresh-context critic passes. The bar as measured at close:
daily / monthly / per-session / 5-hour billing blocks at **0.0000% max cost
delta** against a committed, independently re-verified ccusage 20.0.19
reference — with the fixture deliberately containing the four adversarial
shapes that DIVERGED before they were pinned (missing-token records, stored
costUSD, boundary-exact entries, duplicates). Versioned on-disk pricing
(`as_of`) with cache read/write split across all 12 adapters; blocks with
live burn, projection, and honestly-baselined plan progress in the Studio;
$ per model/project/day; zero outbound calls; CI-guarded
(`costs-bench.yml`) so the round-1 fatal class (uncommitted fixture) can
never ship silently again. Named follow-up: session-view aggregates vs the
usage path (semantics inherent to session-level rollups).
