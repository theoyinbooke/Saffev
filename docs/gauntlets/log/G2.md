# G2 round log — Agent coverage

## Round 1 — 2026-07-28 — the harness (fixtures make the adapter claims testable)

**Builder artifact.** Before this round, 3 of the 5 shipped adapters (Codex,
Cursor, OpenCode) were covered only by `#[ignore]`d smoke tests against real
machine state — untestable claims. Now: `tests/fixtures/agents/<tool>/` holds
one committed, sanitized fixture per tool format — JSONL trees for Claude
Code / Codex / VS Code, and reviewable `.sql` text (materialized into real
SQLite at test time) for Cursor / OpenCode — each with a corrupted variant.
`tests/agents_bench.rs` is the one-command scorer: for every adapter it
proves extraction of the rubric fields (session id, title, project, model,
timestamps, per-message roles, token counts) plus tool-call detection and
non-fatal degradation on the corrupted fixture, and emits
`bench/agents-results.json` — the coverage-table artifact — with a floor
(all 5 complete) so coverage only regresses loudly. Fixture seams
(`with_root` / `with_db` / `with_roots`) added to each reader.

**Honesty rule.** A field the tool's format does not carry is recorded as
`absent_in_format`, never claimed: Cursor and VS Code store no token usage
locally. The Codex fixture pins the OpenAI-convention subtlety (input_tokens
is TOTAL; the adapter must subtract cached) that once overstated costs 10×.

**Measured (this commit):** 5 of 5 adapters complete on the rubric
checklist; corrupted fixtures degrade non-fatally in all 5 formats
(garbage JSONL lines skipped, unparseable SQLite JSON blobs skipped or
zeroed).

**Named gap.** Breadth: 5 tools vs the rubric's ≥ 10 (bar: ccusage's 15).
Next adapters by install base: Gemini CLI, Copilot CLI (`~/.copilot/otel`),
Windsurf, Amp, Goose.

**Critic verdict (fresh-context, judged at f7c0319).** **SOUND.**
Reproducibility PASS (clean clone, one command, byte-identical artifact,
floor verified real). Fixture honesty verified against ACTUAL on-disk stores
on this machine: claude_code/codex record and usage keys match real
transcripts exactly, the Codex token-convention pin is the correct reading
of real data, and the "before" claim (3 of 5 adapters had only ignored smoke
tests) checked out at the parent commit. Six independent probes through the
fixture seams — garbage-only files, empty dirs, an 8 MB line, truncated
SQLite, unicode paths, headerless sessions — produced zero panics and zero
hangs. Rubric: (b) MET, corrupted fixture per adapter; (a) 5 of ≥ 10, as
declared — the harness's checklist and coverage table will support the
breadth rounds, though the floor must be bumped per adapter.

**Findings, all closed in round 2 below:** Cursor's token label should be
`present_but_unpopulated`, not absent — the critic scanned 1,351 real
bubbles: the tokenCount fields exist and are all zero (if Cursor starts
populating them, "absent" would hide it). The SQLite binary-failure path
(truncated db, non-SQLite bytes) was untested by the harness. Real
claude_code transcripts are dominated by noise record types the fixture
omitted (mode, attachment, file-history-snapshot…). The Codex fallback id
kept its timestamp prefix. And the gauntlet's Copilot pointer was wrong:
`~/.copilot/otel` is telemetry counters — transcripts live in
`~/.copilot/history-session-state/`. Adapter order advice: Gemini CLI first
(largest install base), then Windsurf (reuses the Cursor `state.vscdb`
machinery — cheap), then Copilot CLI.

## Round 2 — 2026-07-28 — the critic's harness debts, closed

**Builder artifact.** Token coverage is now three-valued (`Proven` /
`absent_in_format` / `present_but_unpopulated`) with Cursor correctly on the
third; the harness exercises the SQLite binary-failure path (a truncated db
and a text file posing as one must degrade to an empty list for both SQLite
adapters — pinned, no longer just probed); the claude_code fixture
interleaves six realistic noise record types so the unknown-type skip path
runs on real traffic shapes, not just garbage; the Codex fallback id is the
bare trailing uuid (pinned by an exact-id assertion on the corrupted
rollout); GAUNTLETS.md's Copilot pointer corrected and Windsurf promoted per
the critic's install-base reasoning.

**Named gap.** Breadth: 5 of ≥ 10 tools. Next: Gemini CLI, then Windsurf.

**Critic verdict.** _pending — next fresh-context critic run._

## Round 3 — 2026-07-28 — Gemini CLI adapter; Windsurf ruled out honestly

**Builder artifact.** `src/agents/gemini.rs` — the sixth adapter, format
verified verbatim against google-gemini/gemini-cli source (the research
fetched `chatRecordingService.ts` / `chatRecordingTypes.ts` across 10+ tags).
Both format generations parse: the current append-only JSONL (v0.39+ —
metadata first line, `$set` patches, `$rewindTo` truncation, and the
replay semantic that re-appends a whole message when tokens attach, deduped
by id last-wins — summing without the dedupe double-counts) and the legacy
pre-v0.39 single-JSON `ConversationRecord`. Roles are `user`/`gemini`;
tool calls are embedded in the gemini message's `toolCalls`; `thoughts`
records surface as Thinking messages. Tokens follow the Codex convention —
`input` is the TOTAL prompt, `cached` a subset, so non-cached = input−cached;
`thoughts` tokens are generated output and count there. Projects resolve via
the `.project_root` marker (v0.29+ slug dirs), honestly absent for legacy
hash dirs without one. Subagent side threads (`chats/<parent>/…`,
`kind: subagent`) are skipped like Claude Code's `subagents/`. Fixtures pin
all of it: the dedupe re-append, `$rewindTo`, garbage + binary + truncated
lines, the legacy generation, and the subagent skip. Harness floor 5 → 6.

**Windsurf: ruled out for transcripts, with evidence.** The round-1 critic's
"cheap Cursor-style adapter" hypothesis is REFUTED: Cascade conversations
live as per-conversation `.pb` files under `~/.codeium/windsurf/cascade/`,
and since ~March 2025 they are ENCRYPTED (7.95–7.98 bits/byte entropy, no
protobuf wire decode, per-UUID keys, server-validated). Windsurf's
`state.vscdb` holds no chat data. SpecStory investigated and reached the
same verdict ("locked that data away and thrown away the key"). A Windsurf
adapter could honestly provide only a session inventory (UUIDs, mtimes,
sizes) — none of the rubric fields — so it does NOT count toward the
≥ 10-tool bar and is deferred. Notable irony for the moat story: Windsurf
churns history at ~50 conversations AND encrypts it — the strongest
"your tools delete your history" example yet, unreachable by design.

**Measured (this commit):** 6 of 6 adapters complete on the rubric
checklist (bench/agents-results.json).

**Named gap.** Breadth: 6 of ≥ 10. Next by research: Copilot CLI
(`~/.copilot/history-session-state/`), Amp, Goose, Cline, Aider.

**Critic verdict.** _pending — fresh-context critic below._

**Critic verdict (fresh-context, judged at 265f728).** **UNSOUND — two real
semantic bugs, certified by the fixtures.** Reproducibility PASS; most format
claims verified verbatim against the real chatRecordingService.ts (metadata
first line, $set patches, re-append dedupe-by-id, roles, TokensSummary
fields, input-is-total-prompt, subagent nesting, .project_root). But:
**`$rewindTo` is INCLUSIVE** — the real source removes "all messages from
(and including) the specified ID"; the adapter kept the target, over-counting
a turn (and its tokens) per rewind, and the fixtures asserted the wrong
count as correct. **`$set.messages` is a CHECKPOINT** — the real reader
clears and rebuilds the message map; the adapter kept pre-checkpoint turns.
Also: metadata detection should require sessionId AND projectHash with
first-wins identity (a later imposter line rewrote the session id); model
tie-break was nondeterministic (HashMap order — flapped between runs);
`tool` tokens dropped entirely; the legacy .json → .jsonl migration twin
would double-list. Eight probes, zero panics. Rubric: 6 of ≥ 10; the gemini
corrupted fixture is genuinely adversarial but certified the rewind bug.
Next adapter: Copilot CLI.

## Round 4 — 2026-07-28 — the critic's Gemini semantics, fixed at the root

**Builder artifact.** All six findings closed: `$rewindTo` removes the
target too (`drain(pos..)`), pinned by a unit test where the rewound turn
carries tokens that must un-count; `$set.messages` checkpoints clear and
rebuild the message map (pinned: pre-checkpoint tokens vanish); metadata
records require sessionId AND projectHash and identity is first-wins (a
later imposter can update lastUpdated only); model ties break by recency,
deterministically; `tool` tokens (toolUsePromptTokenCount — prompt-side,
NOT included in `input`) count as input; a migrated legacy `.json` whose
`.jsonl` twin exists is skipped (pinned by a twin fixture test). The
corrupted-fixture bench assertion now certifies the CORRECT inclusive
semantics (1 surviving turn, not 2).

**Critic verdict.** _pending — next fresh-context critic run._

## Round 5 — 2026-07-28 — Copilot CLI adapter (both store generations)

**Builder artifact.** `src/agents/copilot.rs` — the seventh adapter. Format
grounded in GitHub's own docs + changelog and five independent working
parsers (the CLI is closed-source; notably, ccusage was NOT usable evidence —
it reads the OTel telemetry export, not sessions). Modern generation
(v0.0.342+): `session-state/<uuid>/events.jsonl` event stream (envelope
`{type,id,parentId,timestamp,data}`) + flat `workspace.yaml` (parsed with a
line splitter — no YAML dependency for a flat file). `user.message` reads
the RAW `content`, not the injected `transformedContent`; the persisted
~56 KB `system.message` is skipped as noise; `"auto"` is a routing mode,
never a model name. Token totals exist ONLY in `session.shutdown`
(separate buckets — `input` is non-cached, Anthropic convention); a
hard-killed session has no shutdown record, so input stays honestly 0 and
output falls back to per-message `outputTokens`. Legacy generation
(pre-0.0.342): flat `session_<uuid>_<ts>.json` with OpenAI-style
`chatMessages` — no token counts exist in that era. Resuming migrates a
legacy session AND PRESERVES the original: listing dedupes by session id,
modern wins (pinned by a twin fixture). Harness floor 6 → 7.

**Named gap.** Breadth: 7 of ≥ 10. Research in hand for Cline (fully
readable — tasks/<id>/ under VS Code globalStorage, taskHistory.json
aggregates) and Aider (honest-partial: markdown transcript, synthesized ids,
k-rounded tokens). Amp + Goose research still running.

**Critic verdict.** _pending — next fresh-context critic run._

## Round 6 — 2026-07-28 — Cline adapter (eighth tool)

**Builder artifact.** `src/agents/cline.rs`, verified against Cline's shipped
v3.89.2 tag (the repo HEAD is mid-refactor — the tag is the honest
reference). Reads all three artifacts per task under VS Code globalStorage
(`saoudrizwan.claude-dev`, all Code flavors): the Anthropic-style
`api_conversation_history.json` transcript (text/thinking/tool_use/
tool_result blocks; Cline's historical XML-ish tool calls inside text blocks
stay text, honestly), `ui_messages.json` (token usage lives in
`api_req_started` messages whose `text` is JSON-stringified payload —
Anthropic convention, tokensIn already excludes cache), and
`state/taskHistory.json` aggregates (title, cwd, modelId, token sums). The
task id IS the start time (millisecond epoch string — pinned). Degradation
is layered: a corrupt transcript still lists from its history row with
tokens/title/project intact and turns honestly 0; a missing history file
falls back to the ui stream. Harness floor 7 → 8.

**Named gap.** Breadth: 8 of ≥ 10. Amp + Goose research still running;
Aider (honest-partial) is the fallback if either disappoints.

**Critic verdict.** _pending — next fresh-context critic run._

## Round 7 — 2026-07-28 — Aider adapter (ninth tool, honest-partial)

**Builder artifact.** `src/agents/aider.rs`. Aider has NO central store —
per-project `.aider.chat.history.md` markdown at each git root — so this
adapter differs in discovery: a bounded walk (depth ≤ 4, 20k-dir cap,
hidden/system/build trees skipped) under the home dir; the measurement
boundary is stated, not hidden. Parsing follows aider's OWN round-trip
rules (`split_chat_history_markdown`, load-bearing for their
`--restore-chat-history`): `# aider chat started at …` delimits sessions,
`#### ` is user (multiline joins into one message), `> ` is announcements
(model lines with suffix stripping — `Weak model:` never mistaken for the
main model; `Tokens: 4.2k sent, …` parsed token-wise so the same-line Cost
clause can't break it; `Applied edit to` = the format's closest tool event),
everything else is assistant text verbatim. Honest limits recorded in the
coverage table's notes: ids synthesized (none exist on disk), tokens
k-rounded above 1000 by aider itself, per-message timestamps absent.
Corruption is definitionally non-fatal (any line is legal markdown) — the
corrupted fixture pins that binary noise and unparseable announcement lines
degrade to assistant text. Harness floor 8 → 9.

**Named gap.** Breadth: 9 of ≥ 10 — one adapter from the bar. Goose next
(verified from source: SQLite `sessions.db`, schema v15), then Amp
(threads/T-*.json, recovered-source verified).

**Critic verdict.** _pending — next fresh-context critic run._

## Round 8 — 2026-07-28 — Goose adapter: the tenth tool, rubric bar MET

**Builder artifact.** `src/agents/goose.rs`, verified entirely from
block/goose source at a pinned commit (schema v15): SQLite `sessions.db`
under the platform data dir (goose migrates its own legacy JSONL into the
db, so the db is the single read surface). Sessions carry `YYYYMMDD_N` ids,
`name` titles, `working_dir`, text timestamps, provider + model from
`model_config_json`; token totals use the `accumulated_*` columns (summed
across compactions — the real session numbers) with current-window
fallback, and the coverage table records honestly that `input_tokens`
follows each provider's own convention as stored. `sub_agent` side threads
and archived sessions are excluded. Message blocks (`text` / `thinking` /
`toolRequest` / `toolResponse`, camelCase-tagged) map to the common model;
an unparseable `content_json` blob degrades to zero blocks. Goose joins the
SQLite binary-corruption gauntlet block. Harness floor 9 → **10**.

**Rubric (a): MET.** 10 tools with fixtures proving the seven fields:
claude_code, codex, cursor, opencode, vscode, gemini, copilot, cline,
aider, goose. Every adapter has a corrupted-fixture test (rubric b held
since round 1).

**Named gap.** Amp (research verified, threads/T-*.json) lands next as
margin above the bar.

**Critic verdict.** _pending — the comprehensive critic runs after Amp._

## Round 9 — 2026-07-28 — Amp adapter: eleventh tool, margin above the bar

**Builder artifact.** `src/agents/amp.rs`, format verified from recovered
original TypeScript in Amp's npm bundles plus two independent working
parsers (ccusage, Block's thread-manager-for-amp). One pretty-printed JSON
per thread at `~/.local/share/amp/threads/T-*.json` (`AMP_DATA_DIR` /
`XDG_DATA_HOME` honored). Per-message assistant `usage` in camelCase —
`inputTokens` excludes cache, `cacheCreation`/`cacheRead` separate; `info`
summary markers are noise; tool results ride user messages as `tool_result`
blocks with `run.status`/`run.result`; project identity from
`env.initial.trees[0]` (file:// URI decoded). The honest boundary is in the
coverage notes: the local dir is a MIRROR — pre-Nov-2025 threads may exist
only on ampcode.com. A truncated thread file is skipped, never fatal.
Harness floor 10 → **11**.

**State: 11 tools, all seven rubric fields proven per tool, corrupted
fixture per adapter, coverage table CI-enforced.** vs the bar: ccusage
parses 15 CLIs for token counts; Saffev reads 11 tools' FULL transcripts —
the differentiator the gauntlet names. Windsurf remains excluded with
evidence (vendor-encrypted).

**Critic verdict.** _pending — comprehensive fresh-context critic below._

**Comprehensive critic verdict (fresh-context, judged at 6563649).**
**BAR MET — batch SOUND.** Reproducibility PASS (clean clone, coverage table
byte-identical, 53 adapter unit tests green). Round-4 Gemini fixes verified
against the REAL upstream source — all three semantics match exactly.
Rubric (a) judged under both readings, explicitly: lenient (extraction
proven to the limit of what each format contains, absences three-valued and
declared) = 11/11; strict-literal = 8–9 (vscode absent, cursor unpopulated,
aider synthesized ids). The critic adopts the lenient reading with
reasoning: "strict demands inventing data the format does not carry" — and
notes even discounting aider entirely the count is 10. Rubric (b) MET for
all 11. Format-fidelity spot-checks (goose vs block/goose main, gemini vs
gemini-cli main): faithful. Six probes — findings, all minor: copilot
single-quoted YAML values kept their quotes; copilot shutdown-only sessions
and amp string-content threads were SILENTLY DROPPED rather than degraded;
aider's fence-unaware ROLE rules are format-owner-faithful (aider itself
does the same) but the fenced session-HEADER over-split was Saffev's own.
Goose session_type filter noted (scheduled/terminal/gateway/acp list as
sessions — acceptable). Most-requested missing tool: **Roo Code** (Cline
fork, near-identical store — the cheapest next adapter); Windsurf remains
the most-requested overall, legitimately ruled out.

## Round 10 — 2026-07-28 — the comprehensive critic's findings, closed

**Builder artifact.** All three fixes with pinning tests: copilot strips
single-quoted YAML values, and a session with recorded token spend but zero
surviving messages LISTS (spend must never vanish — message_count honestly
0); amp bare-string content degrades to one text block instead of
invisibility; aider session-splitting is now fence-aware (a pasted log line
inside a code fence no longer creates a phantom session) while ROLE rules
stay fence-unaware on purpose — aider's own reader is, and diverging would
be less faithful, not more.

**G2 state at close of the breadth push: 11 tools** (claude_code, codex,
cursor, opencode, vscode, gemini, copilot, cline, aider, goose, amp), all
seven rubric fields proven per tool from committed fixtures, corrupted
fixture per adapter, silent-drop class eliminated, coverage table
CI-enforced at floor 11. Named next gap: Roo Code.
