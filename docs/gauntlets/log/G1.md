# G1 round log — Privacy Lens

## Round 1 — 2026-07-27 — the harness (and what it immediately found)

**Builder artifact.** `tests/fixtures/pii/corpus.jsonl` (69 cases: 47 scored
positives+cross, 15+ adversarial traps, 7 xfail breadth targets),
`tests/pii_bench.rs` (one-command scorer with regression floors),
`bench/pii-results.json` (measured baseline).

**Measured baseline (shipped detector, default set):**

| kind | precision | recall | notes |
|---|---|---|---|
| email | 0.889 | 1.0 | FP: `image@2x.png` filename |
| credit_card | 0.889 | 1.0 | FP: Luhn-valid international phone `+234 803 555 1234` |
| api_key | 1.0 | 1.0 | |
| ip_address | 0.667 | 0.727 | **IPv6 compressed forms truncate** (`2001:db8::8a2e:370:7334` → `2001:db8::`) — masking-relevant; `1.2.3.4` version-string FP |
| phone | 1.0 | 0.875 | miss caused by the card detector claiming the Luhn-valid phone |
| **overall** | **0.867** | **0.907** | |

Known gaps (xfail, not yet detected): ssn (miscaught as phone), iban, jwt,
private_key, connection_string (partially miscaught as email), mac_address,
obfuscated email.

**Critic verdict.** _superseded — round 2 landed before a critic ran; the
round-2 critic judges the combined state._

## Round 2 — 2026-07-27 — six new kinds, round-1 FPs fixed

**Builder artifact.** `src/brain/pii.rs`: six new deterministic detectors —
`private_key` (PEM blocks; public keys rejected), `jwt` (three base64url
segments), `connection_string` (credentials-in-URL; outranks email),
`ssn` (dashed form, structural validation), `iban` (ISO 7064 mod-97),
`mac_address` (colon/hyphen forms). All wired through masking, the store,
`/api/agents/privacy`, and Studio labels. Round-1 findings fixed: filename
emails (`image@2x.png`), the Luhn-valid international phone claimed as a card,
IPv6 compressed-form truncation. Corpus grown 69 → 87 cases (34 adversarial
traps, 1 xfail). CI guard added (`.github/workflows/pii-bench.yml`): floors +
stale-baseline check.

**Measured (this commit):**

| kind | precision | recall |
|---|---|---|
| api_key, connection_string, credit_card, email, iban, jwt, mac_address, phone, private_key, ssn | 1.0 | 1.0 |
| ip_address | 0.917 | 1.0 |
| **overall** | **0.984** | **1.0** |

**Named gap.** `[ip-t04]` — bare `1.2.3.4` version-string trap still fires the
ipv4 detector (the round-1 FP that survived). Breadth: 6 of the rubric's ≥ 8 new
kinds landed; `.env`-style assignments, cloud secret formats, and crypto wallet
addresses remain open.

**Critic verdict (fresh-context, judged at 539ec83).** **FAIL — breadth.**
(a) recall ≥ Presidio deterministic: PASS (recall 1.0 everywhere; 1 FP vs 6 in
the shipped baseline). (c) reproducibility: PASS — clean clone, one command,
`git diff --exit-code bench/pii-results.json` clean. (b) new kinds: **FAIL, 6 of
≥ 8**. Corpus honesty findings: dash-less/spaced SSN missing (exactly where
Presidio would win), thin positives (ssn/jwt/private_key n=2), single-line PEM
only, no spaceless E.164 or NANP-dotted phone, no `Authorization: Bearer`
header, no ODBC `Server=…;Password=…;` form. Weakest detector: ip_address
(the ip-t04 version-string FP). Named next gap: breadth — land ≥ 2 more kinds.

## Round 3 — 2026-07-27 — breadth to 8 kinds, critic's corpus gaps closed

**Builder artifact.** Two new kinds: `env_assignment` (`.env`-style credential
assignments — segment-validated key, placeholder/interpolation/entropy-gated
value, defers to specific kinds when the value is itself a prefixed API key or
JWT) and `crypto_wallet` (legacy BTC via **real base58check double-SHA-256**,
bech32/bech32m via **BIP-173/350 polymod**, EVM `0x`+40-hex by shape — git SHAs
and 64-hex tx hashes structurally excluded). Round-2 critic findings all
closed: ip-t04 fixed with a version-context gate (`build/version/release` token
before a dotted quad); SSN dash-less + spaced forms behind explicit SSN
context; ODBC/ADO.NET `Key=Value;…Password=…;` connection strings; spaceless
E.164 and NANP-dotted phones; `Authorization: Bearer` JWT; multi-line PKCS#8 +
EC PEM bodies; thin kinds thickened. Corpus 87 → 119 cases (45 traps).
Corpus-shape floors raised to ≥ 110 scored / ≥ 40 traps; ip_address floor
raised to 1.0/1.0; floors added for both new kinds.

**Measured (this commit):**

| kind | precision | recall | tp |
|---|---|---|---|
| all 13 kinds | 1.0 | 1.0 | 81 total |
| **overall** | **1.0** | **1.0** | fp=0, fn=0 |

New kinds since the round-1 baseline: **8** (private_key, jwt,
connection_string, ssn, iban, mac_address, env_assignment, crypto_wallet).

**Named gap.** EVM addresses are shape-validated only (EIP-55 needs keccak —
not shipped); `.env` recall is bounded by the credential-word list (a secret
under an unlisted key name passes through); obfuscated email (xfail-001)
remains out of deterministic scope.

**Critic verdict (fresh-context, judged at e3693bd).** **PASS.**
(a) PASS — fp=0 fn=0, FP ≤ round-2's 1; nothing Presidio's deterministic
recognizers would catch that Saffev misses on this corpus. (b) PASS — 8 new
kinds verified in the results file. (c) PASS — clean clone, one command,
byte-identical regenerated baseline. Adversarial probes all behaved as
claimed: checksum-invalid base58/bech32 rejected (cross-checked against an
independent double-SHA-256 implementation), `AUTHOR=`/`PUBLIC_KEY=` silent,
`build 1.2.3.4` silent while `ping 1.2.3.4` fires, context-less nine digits
silent, 64-hex tx hash rejected. Honest edges noted, not failures:
the corpus is builder-authored; EIP-55 is not enforced on mixed-case EVM
addresses (keccak not shipped); per-kind positives are thin (3–6).

**Next gap (critic-named).** Structured-config secret detection — the
env_assignment grammar is shell `NAME=value` only; `{"password": "…"}` JSON
and `key: value` YAML secrets produce zero findings today. Largest
real-world credential surface the suite cannot see; the round-4 target,
with corpus cases and traps, plus an EIP-55 trap once keccak lands.

## Round 4 — 2026-07-27 — structured-config secrets + EIP-55

**Builder artifact.** The round-3 critic's named gap closed: the config-secret
grammar (kind `env_assignment`, display "Config credential") now reads JSON
members (`"password": "…"`), YAML mappings (`password: …`, comment-aware), and
TOML spaced assignments (`password = "…"`) alongside shell — all four forms
share the key-segment and value gates, and all defer to the specific kinds
when the value is itself a prefixed API key or JWT (a JWT inside a JSON value
keeps its `jwt` label). EIP-55 landed via a real Keccak-256 dependency
(`sha3`): a mixed-case EVM address must match the digest-derived casing
exactly; the round-3 critic's case-flip probe is now a corpus trap and a unit
test. Coverage thickened: FR + ES IBANs, a fourth JWT. Corpus 119 → 130 cases
(49 traps); shape floors raised to ≥ 120 scored / ≥ 45 traps.

**Measured (this commit):** precision 1.0, recall 1.0, fp=0 fn=0, tp=88
across 13 kinds.

**Named gap.** Corpus remains builder-authored (the standing caveat); IBAN
still covers 5 of ~80 countries; obfuscated email stays the lone xfail.

**Critic verdict (fresh-context, judged at 6260c0b).** **PASS on the rubric,
with a constructed FP class.** (a)/(b)/(c) all PASS — fp=0 fn=0 across 13
kinds, byte-identical reproduction from a clean clone. 22 adversarial probes:
16 behaved as claimed (EIP-55 case-flip silent, JWT-in-JSON deferral works,
`warning:`/`note:`/`12:30`/URLs all silent). **3 constructed FPs**: the YAML
branch fires on line-start prose whose first word is a secret keyword —
`token: expired yesterday`, `password: incorrect, try again`,
`secret: the cake is a lie` — the value gate passes any English phrase.
**3 constructed misses**: `DB_PASS=…` (PASS not in the word list),
single-quoted TOML values, YAML block scalars. Corpus covers the round-4
surface but has no prose-colon trap with a secret-named key — precision 1.0
is real on the corpus but would not survive chat-shaped traffic.

**Next gap (critic-named).** A natural-language value rejector for the YAML
form, plus prose-colon traps so the gap is scored, not asserted — the round-5
target.

## Round 5 — 2026-07-27 — the prose-colon FP class, killed and scored

**Builder artifact.** All six round-4 critic constructions answered. The FP
class: the YAML form now rejects unquoted values containing whitespace — a
real YAML secret is a single token or a quoted string, so `token: expired
yesterday` is prose by construction, while `password: "correct horse battery
staple"` (quoted, deliberate) still fires. The misses: `PASS` joined the
key-segment list (`DB_PASS=…` now caught; `FEATURE_BYPASS` still can't
qualify — segments, not substrings), and TOML accepts single-quoted values.
All three critic FPs, both fixed misses, the quoted-multi-word positive, and
a BYPASS trap are corpus cases now — the gap is scored, not asserted. Corpus
130 → 137 cases (53 traps); shape floors raised to ≥ 130 scored / ≥ 50 traps.

**Measured (this commit):** precision 1.0, recall 1.0, fp=0 fn=0, tp=91
across 13 kinds.

**Known remaining edge (named, unfixed).** YAML block scalars (`password: |`
+ indented next line) stay invisible — a multi-line grammar, deliberately out
of the single-line candidate model for now. Corpus remains builder-authored.

**Critic verdict (fresh-context, judged at 4376c37).** **PASS.** (a)/(b)/(c)
all hold — byte-identical reproduction, fp=0 fn=0 across 13 kinds, 8 kinds
landed since baseline (exactly at the bar; the critic flags that only 6 are
kinds Presidio lacks, under a stricter reading of "new"). All six round-5
claims verified true and present as corpus cases. Novel constructions found
the gate leaking in both directions: **one unambiguous FP** —
`password: [FILTERED]` / `[REDACTED]` (Rails log-scrubbing convention) fires;
single-token status words (`password: incorrect`, `token: expired`) fire on an
entropy accident (2.73 vs the 2.0 threshold) while `password: reset` (1.92)
happens to be silent; Ruby hash-rocket secrets (`"password"=>"…"`) are a
missed grammar. Quoted prose (`secret: "we will ship on friday"`) judged
fail-closed-correct, not an FP.

**Next gap (critic-named).** The value-side gate: reject bracketed redaction
markers and a named status-word list instead of leaning on the entropy
accident; cover the hash-rocket grammar — the round-6 target.

## Round 6 — 2026-07-27 — value-gate rationalized, hash-rocket grammar

**Builder artifact.** The round-5 critic's three findings closed. Bracketed
redaction markers (`[FILTERED]`, `[REDACTED]` — any `[`-prefixed value) are
rejected by the value gate, alongside a named status-word list (incorrect,
expired, invalid, missing, required, hidden, unknown, unset, filtered) so the
single-token prose boundary is a decision, not an entropy accident — this
fixes the same class in the shell form (`export PASSWORD=incorrect`). Ruby
hash-rocket members (`"password"=>"hunter2secret99"`) joined the config-secret
grammar pool. All three are corpus cases now.

**Measured (this commit):** precision 1.0, recall 1.0, fp=0 fn=0 across 13
kinds; corpus 137 → 140 cases (55 traps).

**Critic verdict (fresh-context, judged at 86ed87c).** **PASS (conditional).**
(c) PASS — byte-identical reproduction from a clean clone. (a) PASS on the
corpus — fp=0 fn=0 across 13 kinds. (b) **MARGINAL FAIL under a strict
reading**: 13 kinds scored, but only 6 are beyond Presidio's entity list
(api_key, jwt, private_key, env_assignment, connection_string, mac_address);
7 overlap (crypto_wallet ≈ Presidio's CRYPTO). Earlier critics read "≥ 8 new
kinds landed" as landed-during-gauntlet (8 ✓); this reading is now flagged
twice and needs settling in the gauntlet text. All three round-6 claims
verified true and corpus-backed; status-word list confirmed case-insensitive;
`password: hidden2secret` / `expired1` correctly still fire (substrings don't
kill real secrets).

**New constructions — the denylist regrows one decoration away.** 3 FPs:
`password: ***MASKED***`, `password: (REDACTED)` (decorated markers escape the
exact-match and `[`-prefix checks), `token: invalid_token` (morphological
status compounds). 2 FNs: `:password=>"…"` (symbol-key Ruby rocket — the
older, very common form; the shipped grammar requires a quoted key) and
`PASSWORD='[k9!fQ2xW8z'` (the blanket `[`-prefix kill eats a quoted real
secret — prefix kills should only apply to unquoted values).

**Next gap (critic-named).** Fix the gate's **shape**, not its list:
normalize the value before the placeholder check (strip surrounding
punctuation/decoration, lowercase, then match marker words and `_`-joined
status compounds), scope the prefix kills to unquoted values, and add the
symbol-key rocket grammar — one principled change that kills all three FPs
and the clearest FN. Also: settle the rubric's "new kinds" reading, or land
two more Presidio-absent classes (e.g. cloud secret formats) to moot it.

## Round 7 — 2026-07-27 — the gate's shape, not its list

**Builder artifact.** All five round-6 critic findings closed by changing the
gate's structure. The value gate now normalizes before matching — quotes
stripped, surrounding decoration trimmed, lowercased — so `***MASKED***`,
`(REDACTED)`, and `[FILTERED]` all resolve to their marker word in any
grammar; `_`/`-`-joined compounds reject only when EVERY segment is known
non-material vocabulary (`invalid_token`, `token_expired`, `access_denied`
die; `secret_dragon` lives). The `[`/`<` prefix kills now apply to unquoted
values only, and grammar-quoted forms (JSON/rocket/TOML) carry their quoting
into the gate — so `PASSWORD='[k9!fQ2xW8z'` fires in every form. Symbol-key
Ruby rockets (`:password=>"…"`) joined the grammar pool, and both rocket
forms accept single quotes. The rubric's "new kinds" reading is settled in
GAUNTLETS.md: new = not shipped at gauntlet open (the build list itself
includes Presidio-overlapping kinds). All five findings are corpus cases;
corpus 140 → 145 (58 traps), shape floors 140/57.

**Measured (this commit):** precision 1.0, recall 1.0, fp=0 fn=0, tp=94
across 13 kinds.

**Critic verdict (fresh-context, judged at 4f7cde1).** **PASS.** (c) PASS —
byte-identical clean-clone reproduction. (b) PASS at zero margin — exactly 8
new kinds under the clarified reading, which the critic endorses ("the build
list itself names SSN/IBAN/MAC"). (a) PASS on the corpus, with the standing
caveat sharpened: **Presidio has never actually been executed against this
corpus** — recall 1.0 makes the comparison vacuously true; the competitor's
score is assumed, not measured. All five round-7 fixes verified structural
and corpus-anchored; no over-kill found on real secrets (`Error404!x9`,
`denied99x`, hex keys all fire; killing `password: 'null'` judged right).

**New constructions.** Clear FPs: `serial 1111-2222-3333` / `0000-0000-0000`
claimed as phone (all-uniform digit groups pass plausibility);
`token: not_set` / `password: to_be_filled` (filler connectors aren't
vocabulary); `PASSWORD=REDACTED_BY_SOC_TEAM` / `password: "MASKED (by
proxy)"` (marker-plus-annotation defeats normalization). Clear FN class:
mid-line unquoted-key object literals — `{password: "hunter2xyz99"}` (JS),
`opts = {password: 'railsKw99x'}` (Ruby 1.9) — covered by no grammar, and
"arguably the single most common secret-paste shape in logs."

**Next gap (critic-named).** The object-literal grammar (JS / Ruby 1.9 /
Python dict repr), the phone plausibility gate, the annotation-marker rule —
and, methodologically, running Presidio for real against the corpus so (a)
is measured rather than assumed.

## Round 8 — 2026-07-27 — object literals, phone plausibility, Presidio measured

**Builder artifact.** All round-7 critic findings closed. FN class: a new
object-literal grammar (unquoted key + QUOTED value, anywhere on a line)
reads JS `{password: "…"}` and Ruby 1.9 `{password: '…'}`; the JSON grammar
takes single quotes for Python dict reprs. The quoted value is load-bearing —
mid-line unquoted prose stays out of scope. FPs: phone candidates where every
digit group is one repeated digit (`1111-2222-3333`, `0000-0000-0000`) are
serials, not numbers; filler connectors (`not_set`, `to_be_filled`) joined
the non-material vocabulary; values whose FIRST segment is a scrub verb
(`REDACTED_BY_SOC_TEAM`, `MASKED (by proxy)`) are annotated markers — while
`was-removed-x9q` stays material. Corpus 145 → 153 cases (63 traps), floors
148/60.

**Presidio, measured.** `scripts/presidio_baseline.py` runs presidio-analyzer
against the same corpus (`bench/presidio-baseline.json`) — the rubric's (a)
comparison is now scored on both sides. Result: Saffev recall ≥ Presidio on
every overlapping kind, strictly better on five — Presidio missed 7 positives
(both EVM wallets, all three non-canonical SSN forms + one dashed, the
Spanish IBAN, a spaced E.164 phone) and hit 4 traps (the `1.2.3.4` version
string, a date range, a bare digit run, an invalid-area SSN) where Saffev has
fp=0. The round-2 critic's assumption that "Presidio would win on dash-less
SSN" is measured false.

**Measured (this commit):** Saffev precision 1.0, recall 1.0, fp=0 fn=0,
tp=97 across 13 kinds on 153 cases / 63 traps.

**Critic verdict (fresh-context, judged at adf9013).** **PASS**, with two
honesty corrections and one real bug. (c) PASS — both artifacts reproduce
byte-identical, including a rerun of the Presidio baseline. (b) PASS — 8 new
kinds verified against the gauntlet-open commit. (a) PASS on the letter, but:
**"strictly better on five" overcounts** — recall is strictly better on
exactly 4 kinds (the fifth is an ip_address precision win); and **the SSN
margin is a denylist artifact** — 3 of 5 SSN positives are the Woolworth
number `078-05-1120`, which Presidio explicitly denylists as a known fake;
probed with a realistic SSN, Presidio catches all three formats. The EVM
wallet and Spanish IBAN misses are genuine Presidio gaps (verified). The
methodology is otherwise fair, mildly lenient TO Presidio; the small-spacy
caveat is immaterial (all scored entities are pattern/checksum recognizers).

**Constructions.** One span-correctness bug: `password_hint: "mother's maiden
name"` — the quote-class regexes open on `"` and close on the apostrophe,
mangling the span (masking would leave `s maiden name"` exposed). FNs:
escaped quotes (`{password: "hu\"nter99"}`) and JS template literals both
invisible. FPs: `1212-1212-1212` and `1111-2222-3334` fire as phone (the
uniform-repeat rule misses the 4-4-4 serial shape); `password: "TBD-final"`
(filler-plus-annotation). No over-kill found; `was-removed-x9q` verified
material but only by probe — no corpus case backs it.

**Next gap (critic-named).** Paired-quote value scanning — open-quote kind
must equal close-quote kind, with escape handling — which fixes the mangled
span, the escaped-quote FN, and template literals in one move. Plus: swap the
denylisted SSN for realistic fakes and re-measure Presidio.

## Round 9 — 2026-07-27 — paired quotes, serial shapes, honest Presidio

**Builder artifact.** The round-8 critic's findings closed at the root. The
span bug: every quoted-value grammar now pairs quotes exactly (spelled-out
alternation per quote kind — the regex crate has no backreferences), so
`password_hint: "mother's maiden name"` spans the full pair; double-quoted
branches accept backslash escapes (`"hu\"nter99x"` caught) and the
object-literal form takes JS template literals — the claim loop reads
captures positionally (first participating group = key, second = value).
Phone plausibility now also rejects all-identical groups (`1212-1212-1212`)
and the 4-4-4 quad shape (`1111-2222-3334`). Filler leads (`TBD-…`,
`TODO-…`) join the scrub verbs in the first-segment rule; `was-removed-x9q`
stays material and is now a corpus case. Corpus 153 → 160 cases (66 traps),
floors 155/63.

**Presidio, honestly.** The denylisted Woolworth SSN was swapped for a
realistic fake (`519-30-4297`) in all three formats and the baseline
re-measured: Presidio's SSN recall is 1.0 on realistic data, exactly as the
critic predicted — the corrected claim is **recall strictly better on 3
kinds** (phone 0.9, iban 0.833, crypto_wallet 0.667), tied at 1.0 elsewhere,
and **0 trap hits vs Presidio's 5** (dates, a bare digit run, the `1.2.3.4`
version string, a never-issued-area SSN, and the context-less bare nine
digits where Saffev's context gate holds).

**Measured (this commit):** Saffev precision 1.0, recall 1.0, fp=0 fn=0
across 13 kinds on 160 cases / 66 traps.

**Critic verdict (fresh-context, judged at 3634f4c).** **PASS.** (c) PASS —
both artifacts byte-identical from a clean clone, Presidio baseline rerun
included. (a) PASS — the corrected "strictly better on exactly 3, tied
elsewhere, 0 vs 5 trap hits" verified exactly right. (b) PASS — 8 new kinds
against the gauntlet-open commit. All seven round-9 claims verified.

**Constructions.** One severe FN: the **deferral hand-off leak** —
`PASSWORD=eyJ0eXBlIjoiYWNjb3VudCJ9` (base64 of `{"type":"account"}`; every
base64-encoded JSON credential starts `eyJ`) produces NOTHING: env defers on
the `eyJ` prefix, the JWT detector never claims (no dots). Same family:
`PASSWORD=sk-…` below the entropy gate — env defers on the prefix match, the
api-key gate rejects, total silence. The deferral skips unconditionally
instead of checking the hand-off will be caught. Also: the escaped-quote fix
was asymmetric (TOML double-quote FN, shell + single-quote branches still
truncate spans — the round-8 bug family alive in three grammars);
`password: rejected` / `token: unauthorized` (vocab synonyms regrow);
`GPG_PASSPHRASE=…` invisible (PASSPHRASE not a key word); lowercase IBAN
missed; `+7 777 777 7777` (plausible Kazakh mobile) over-killed by the
all-repeated-groups rule.

**Next gap (critic-named).** Fix the deferral hand-off — only skip an env
candidate when the deferred-to detector will actually claim it. The one
confirmed silent total leak of a real credential shape.

## Round 10 — 2026-07-27 — the deferral hand-off, verified before skipping

**Builder artifact.** All round-9 critic findings closed. The severe FN: the
config-secret deferral now verifies the hand-off — an env candidate is
skipped only when the value actually matches the JWT shape (`RE_JWT`, dots
required) or would clear the api-key gates (`api_key_accepts`, the same
body-length + entropy helper the api-key step itself uses) — so
`PASSWORD=eyJ0eXBlIjoiYWNjb3VudCJ9` (base64 JSON) and below-entropy `sk-…`
values are claimed as config credentials instead of vanishing, while real
keys and JWTs keep their specific labels. Escaped-quote handling is now
symmetric: shell and TOML double-quoted branches take backslash escapes, and
the single-quoted JS/Ruby branches do too (TOML/shell single-quote strings
are literal by their own specs). Status-vocab synonyms (rejected,
unauthorized, forbidden, wrong, mismatch) join the non-material list;
PASSPHRASE joins the credential key words; lowercase compact IBANs are
recognized; a `+` country prefix exempts degenerate-looking real numbers
(`+7 777 777 7777`) from the serial-shape rules.

**A bench catch worth recording.** The first lowercase-IBAN attempt used a
blanket `(?i)` — the greedy spaced-group tail then glued the following word
into the candidate (`ES91… activa` absorbed `acti`+`va`, failed mod-97, and
the true IBAN vanished). The committed corpus caught it before it shipped;
the fix keeps spaced groups uppercase-only and lowercase compact-only.

**Measured (this commit):** precision 1.0, recall 1.0, fp=0 fn=0, tp=108
across 13 kinds on 169 cases / 68 traps. Presidio baseline re-measured on the
grown corpus: unchanged (4 misses, 5 trap hits).

**Critic verdict (fresh-context, judged at fda03f7).** **PASS.** All three
rubric legs reproduce from a clean clone (Presidio baseline byte-identical on
rerun); all round-10 claims verified precisely, including the deferral
boundary case (`PASSWORD=sk-…` at entropy 2.05 — between env's 2.0 gate and
api_key's 3.0 — correctly claimed as env). ~40 constructions across three
waves; the config-secret family held for the first time in six rounds. Two
defects, both outside the corpus: **MAC right-boundary glue** —
`00:11:22:33:44:55:66` (a 7-group run) fires mac_address on its first six
pairs, exactly the glue class the IBAN fix set as the standard — and
**empty-user URL credentials** — `redis://:p4ssw0rd@cache:6379/0`, the
standard Redis AUTH form, fires nothing because the URL grammar requires a
non-empty user. Also noted, judged acceptable: `SECRET=4242…` claims as
credit_card (sane, unpinned); all-`a` `sk-` placeholder suppressed by env's
own entropy gate.

**Next gap (critic-named).** Empty-user URLs (higher real-world hit rate)
plus the MAC boundary guard — one round for both.

## Round 11 — 2026-07-27 — MAC glue guard, empty-user URLs

**Builder artifact.** Both round-10 findings closed. The URL connection-string
grammar accepts an empty user (`scheme://:pass@host` — Redis AUTH). MAC
candidates glued to a further `:`/`-` hex group on either side are fragments
of a longer run, not addresses ([`mac_boundaries_clean`] — the IBAN glue
standard applied). The unpinned claim-precedence case is pinned: an
env-shaped value that is itself a card number is claimed as `credit_card`
(env's entropy gate passes on it, the card detector claims the digits).

**Critic verdict.** _pending — next fresh-context critic run._
